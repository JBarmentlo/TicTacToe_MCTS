#include "head.h"
#include "stdio.h"

int main(void) 
{
	make_perfect_db();
	printf("sdfsdf");
	score();

/*
	int it = 1;
	
	while (it <= 20)
	{
		learn(2000);
		show_game();
		printf("trained for %d games\n", (it * 2000));
		it++;
	}
	
	play_against_AI();
	*/
  return (0);
}
