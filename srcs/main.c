#include "head.h"

int main(void) {
	char *board;

	int move;

	int turn;
	int it = 1;

	while (it <= 5)
	{
		learn(2000);
//		show_game();
//		printf("trained for %d games\n", (it * 2000));
		it++;
	}
//	play_against_AI();
  return (0);
}

